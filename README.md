# Apple-Disease-Classification-Project
This project focuses on identifying diseases in apple leaves using a Convolutional Neural Network (CNN) classifier. The application allows users to upload an image of an apple leaf, processes it, and predicts the disease category.
